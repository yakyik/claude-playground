apiVersion: v1
kind: Service
metadata:
  name: bastion-mcp-server
  namespace: bastion
  labels:
    app: bastion-mcp
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: bastion-mcp
